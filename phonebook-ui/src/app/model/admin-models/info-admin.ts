export interface InfoAdmin {
  activateTokens: number,
  addresses: number,
  contacts: number,
  emails: number,
  phones: number,
  recoveryTokens: number,
  users: number,
  activatedUsers: number;
}
